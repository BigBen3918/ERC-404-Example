{
  "name": "erc404_generate",
  "version": "0.0.1",
  "scripts": {
    "test": "npx hardhat test",
    "deploy_fantomtest": "npx hardhat run --network fantomtestnet scripts/deploy.ts",
    "deploy_sepoliatest": "npx hardhat run --network sepolianet scripts/deploy.ts",
    "deploy_bsctest": "npx hardhat run --network bsctestnet scripts/deploy.ts",
    "prettier": "prettier --write --plugin=prettier-plugin-solidity 'contracts/**/*.sol'"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.6",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.10",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.20.1"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-verify": "^2.0.4",
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.4.5"
  }
}
